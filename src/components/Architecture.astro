---
import { t } from '../i18n';
const L = t(Astro.currentLocale).architecture;
// Iconos por índice — alineados con L.nodes.
const icons = [
  'M3 12h3l2-7 4 14 2-7h3',
  'M4 4h16v16H4z M9 4v16 M15 4v16',
  'M7 7h10v10H7z M2 10h2 M2 14h2 M20 10h2 M20 14h2 M10 2v2 M14 2v2 M10 20v2 M14 20v2',
  'M5 12a10 10 0 0 1 14 0 M8.5 15.5a5 5 0 0 1 7 0 M12 19h.01',
  'M4 5h16v6H4z M4 13h16v6H4z M8 8h.01 M8 16h.01',
  'M3 17c3 0 3-10 6-10s3 10 6 10 3-7 6-7',
  'M5 6a2 2 0 1 0 0-.01 M5 18a2 2 0 1 0 0-.01 M19 12a2 2 0 1 0 0-.01 M7 7l10 4 M7 17l10-4',
  'M3 4h18v12H3z M8 20h8 M12 16v4',
];
const nodes = L.nodes.map((n, i) => ({ ...n, icon: icons[i] }));
---

<section id="arquitectura" class="relative border-b border-line bg-canvas-2">
  <div class="mx-auto max-w-6xl px-6 py-24">
    <p class="reveal kicker">Signal Chain</p>
    <h2 class="reveal mt-3 text-3xl font-bold tracking-tight sm:text-4xl">
      {L.h2}
    </h2>
    <p class="reveal mt-4 max-w-3xl text-lg text-muted">
      {L.intro}
    </p>

    <!-- Riel de instrumentación: scroll-x en móvil, fila en desktop -->
    <div class="reveal mt-14 -mx-6 overflow-x-auto px-6 pb-4">
      <ol class="flex min-w-max items-stretch gap-0" data-stagger>
        {
          nodes.map((n, i) => (
            <>
              <li class="reveal card-lift instrument group flex w-[210px] flex-none flex-col p-4 shadow-soft">
                <div class="mono mb-3 flex items-center justify-between text-[10px] uppercase tracking-widest text-signal">
                  <span>[ {String(i + 1).padStart(2, '0')} ]</span>
                  <span class="inline-flex h-9 w-9 items-center justify-center rounded border border-line bg-surface-2 text-signal transition group-hover:border-signal">
                    <svg
                      viewBox="0 0 24 24"
                      fill="none"
                      stroke="currentColor"
                      stroke-width="1.6"
                      stroke-linecap="round"
                      stroke-linejoin="round"
                      class="h-5 w-5"
                    >
                      <path d={n.icon} />
                    </svg>
                  </span>
                </div>
                <p class="text-sm font-semibold leading-snug text-ink">{n.name}</p>
                <p class="mt-1 text-xs leading-relaxed text-muted">{n.desc}</p>
              </li>
              {i < nodes.length - 1 && (
                <li class="flex flex-none items-center self-center px-1" aria-hidden="true">
                  <svg width="34" height="16" viewBox="0 0 34 16" fill="none">
                    <line x1="2" y1="8" x2="26" y2="8" stroke="var(--color-signal)" stroke-width="2" class="flow-line" />
                    <path d="M24 4l6 4-6 4" stroke="var(--color-signal)" stroke-width="2" fill="none" />
                  </svg>
                </li>
              )}
            </>
          ))
        }
      </ol>
    </div>

    <!-- Figura opcional: transición de datos / capas IoT. Suelta el archivo y aparece. -->
    <figure class="reveal mt-14 overflow-hidden rounded-2xl border border-line bg-surface shadow-soft">
      <img
        src="/images/transicion-datos.png"
        alt={L.figAlt}
        class="block w-full"
        loading="lazy"
        onerror="this.closest('figure').remove()"
      />
      <figcaption class="border-t border-line px-4 py-3 text-sm text-muted">
        {L.figCaption}
      </figcaption>
    </figure>
  </div>
</section>
