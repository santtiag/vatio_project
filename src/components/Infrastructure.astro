---
import { t } from '../i18n';
const L = t(Astro.currentLocale).infrastructure;
const imgs = ['/images/bloque-a.jpg', '/images/bloque-f.jpg'];
const bloques = L.bloques.map((b, i) => ({ ...b, img: imgs[i] }));
---

<section id="infraestructura" class="border-b border-line bg-canvas">
  <div class="mx-auto max-w-6xl px-6 py-24">
    <p class="reveal kicker">Sites</p>
    <h2 class="reveal mt-3 text-3xl font-bold tracking-tight sm:text-4xl">
      {L.h2}
    </h2>
    <p class="reveal mt-4 max-w-3xl text-lg text-muted">
      {L.intro}
    </p>

    <div class="mt-14 grid gap-6 sm:grid-cols-2" data-stagger>
      {
        bloques.map((b) => (
          <div class="reveal card-lift instrument overflow-hidden shadow-soft">
            <figure class="relative aspect-[16/9] bg-surface-2">
              <img
                src={b.img}
                alt={b.alt}
                class="absolute inset-0 h-full w-full object-cover"
                loading="lazy"
                onerror="this.style.display='none'"
              />
              <figcaption class="mono absolute left-3 top-3 rounded border border-line bg-surface px-2.5 py-1 text-[11px] font-medium uppercase tracking-wider text-ink">
                {b.nombre}
              </figcaption>
            </figure>
            <div class="p-6">
              <div class="flex items-center justify-between">
                <h3 class="text-xl font-semibold">{b.nombre}</h3>
                <span class="mono inline-flex items-center gap-2 text-[11px] font-medium uppercase tracking-wider text-signal">
                  <span class="pulse-dot h-2.5 w-2.5 rounded-full bg-signal" />
                  {L.activeBadge}
                </span>
              </div>
              <p class="mono mt-4 text-[11px] uppercase tracking-widest text-muted">{L.loadLabel}</p>
              <p class="text-base font-medium text-ink">{b.carga}</p>
              <p class="mt-4 text-sm leading-relaxed text-soft">{b.detalle}</p>
              <p class="mono mt-4 text-[11px] uppercase tracking-wider text-signal">{b.estado}</p>
            </div>
          </div>
        ))
      }
    </div>

    <p class="reveal mt-6 flex items-center gap-2 text-sm italic text-muted">
      <svg class="h-4 w-4 flex-none" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.6">
        <rect x="5" y="11" width="14" height="9" rx="2" /><path d="M8 11V8a4 4 0 0 1 8 0v3" />
      </svg>
      {L.privacyNote}
    </p>
  </div>
</section>
